# BloodConnect Backend

## Setup
1. Create `.env` from `.env.example` and set:
   - `API_KEY`
   - Either `GOOGLE_APPLICATION_CREDENTIALS` (path to service account file) or `GCP_SA_KEY` (inline JSON)

2. Install and run:
```bash
npm install
npm run start